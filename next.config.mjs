/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/juanjbsalas',
  trailingSlash: true,
  images: {
    unoptimized: true,
  },
}

export default nextConfig
